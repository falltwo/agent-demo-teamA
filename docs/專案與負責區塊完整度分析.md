# 專案完整度與「RAG／記憶／合約審閱」負責區塊分析

---

## 一、專案目前完整度（整體）

### 1.1 專案定位與競賽主題

- **原始範本**：知識庫問答 + 多工具 Agent 路由（RAG、網路搜尋、圖表、公司工具、專家）。
- **競賽主題**：**分析法律合約的助手**（合約／採購法遵審閱）。
- **現狀**：在範本基礎上已疊加「合約法遵」角色、雙 Prompt（調查員 + 判官）、多查詢檢索、contract_risk_agent；README 仍以範本口吻為主，尚未改寫為「合約審閱助手」產品說明。

### 1.2 已具備的模組與功能

| 區塊 | 狀態 | 說明 |
|------|------|------|
| **RAG 核心** | ✅ 完整 | LangGraph：retrieve → package（調查員）→ generate（判官）；Query Rewriting、多查詢檢索（可選）、rerank、strict/非 strict。 |
| **Agent 路由** | ✅ 完整 | 工具選擇（含 contract_risk_agent、財報/ESG/資料分析專家）、澄清流程、圖表確認。 |
| **專家** | ✅ 完整 | contract_risk_agent（合約法遵）、financial_report、esg、data_analyst；合約專家具 15 年法務審查 prompt 與 strict/顧問雙模式。 |
| **前端** | ✅ 完整 | Streamlit：多對話、上傳灌入、Eval 運行記錄／批次結果、高/中/低優先優化均已實作（含空狀態、篩選區、灌入成功文案、淺色主題、對話標題、Eval 副標、CSS 抽檔）。 |
| **Eval** | ✅ 完整 | eval_log、run_eval、Streamlit 檢視、載入 Run spinner。 |
| **文件** | ✅ 有 | RAG_記憶與上下文.md、FRONTEND_設計分析與優化建議.md、實作紀錄；README 仍為範本版。 |

### 1.3 可再補強之處（整體）

- **README**：若對外呈現為「合約審閱助手」，建議增修一節：競賽主題、合約法遵流程（調查員 + 判官）、建議 env（RAG_USE_HISTORY_FOR_QUERY、RAG_MULTI_QUERY 等）。
- **Eval 題集**：若有合約專用 eval 題（條款指代、交叉比對、風險等級），可補 `eval/eval_set_contract.json` 或類似，方便量測合約場景。
- **串流輸出**：目前回答為一次性生成；若需「逐字出現」需改後端 + 前端（文件已分析，屬可選）。

---

## 二、你負責區塊的完整度：「使用者問題 → RAG → 記憶／回憶」

你負責的是：**讓系統記得上下文**（多輪對話、指代、情境），並在**合約法遵審閱**場景中落實。以下依「記憶／回憶」與「合約審閱」分項檢視。

### 2.1 記憶與回憶（記得上下文）

| 項目 | 狀態 | 說明 |
|------|------|------|
| **History 來源** | ✅ 完整 | Streamlit `current_conv["messages"]` → `history_for_model`，每輪傳入 `route_and_answer(history=...)`。 |
| **History 傳遞** | ✅ 完整 | agent_router 將 history 傳給 run_rag、_decide_tool、各專家；rag_graph state 含 history，package/generate 皆使用。 |
| **History 長度控制** | ✅ 完整 | `RAG_MAX_HISTORY_TURNS`（預設 12），rag_graph generate、package 與 expert_agents 皆只取最近 N 輪。 |
| **指代／情境指示** | ✅ 完整 | 判官與合約專家 system 均明確要求「結合對話歷史理解目前問題的指代與情境」。 |
| **Query Rewriting** | ✅ 完整 | `RAG_USE_HISTORY_FOR_QUERY=1` 時，retrieve 前用 LLM 將短問／代名詞改寫成完整檢索問句，避免「那下一條呢？」無效檢索。 |
| **文件** | ✅ 完整 | `docs/RAG_記憶與上下文.md` 已說明流程、各層用法、環境變數與雙 Prompt。 |

**小結**：**記憶／回憶**端到端已接好（UI → 路由 → RAG → 專家），長度、指代、Query Rewriting 與文件皆到位。

### 2.2 合約法遵審閱（主 RAG + 專家）

| 項目 | 狀態 | 說明 |
|------|------|------|
| **主 RAG 雙 Prompt** | ✅ 完整 | 調查員（package）：法務檢索專家，產出「原始條文脈絡」+「知識庫參考依據」→ `packaged_context`。判官（generate）：僅依 packaged_context 做風險判定。 |
| **調查員 Alignment** | ✅ 完整 | 三類脈絡：交叉比對、模糊字眼、極端情境；無檢索內容時 pass through。 |
| **判官規則** | ✅ 完整 | Strict／Advisor 兩套 system，含指代消解、嚴格守法、細節回憶、來源標記。 |
| **contract_risk_agent** | ✅ 完整 | 15 年資深法務審查 prompt（Role/Task/Guidelines/Output Format），Strict 與顧問雙模式；路由已註冊。 |
| **多查詢檢索** | ✅ 完整 | `RAG_MULTI_QUERY=1` 時依主問句產輔助問句、多查併合去重，補足交叉比對所需脈絡（如管轄法院 + 雙方地址）。 |

**小結**：**合約審閱**主 RAG（調查員 + 判官）與合約專家、多查詢檢索皆已實作並可透過 env 開關。

### 2.3 前端與你負責區塊的介面

| 項目 | 狀態 | 說明 |
|------|------|------|
| **對話標題** | ✅ 完整 | 第一則問題自動取前 20 字為標題。 |
| **Eval 副標** | ✅ 完整 | Eval 頁改為情境化小標（檢視執行記錄／檢視批次結果）。 |
| **CSS** | ✅ 完整 | 自訂樣式抽成 `assets/custom.css` 並分節註解。 |

上述與「記憶／合約」體驗相關的前端項目均已完成。

### 2.4 你負責區塊尚可選補強（非必須）

- **RAG_記憶與上下文.md**：可加一節「合約場景建議」：建議開啟 `RAG_USE_HISTORY_FOR_QUERY=1`、合約審閱時可開 `RAG_MULTI_QUERY=1`、`RAG_MAX_HISTORY_TURNS` 至少 6～12。
- **README**：若希望對外一致呈現「合約審閱助手」，可補一段「合約法遵流程」與相關 env 說明（與你負責的 RAG／記憶／合約邏輯對齊）。

---

## 三、總評

- **專案整體**：功能面已具備 RAG、多工具、合約審閱、Eval、前端優化；README 與合約專用 Eval 題集可視需求補齊。
- **你負責的部分（使用者問題 → RAG → 記憶／回憶 + 合約法遵）**：  
  **記憶與回憶**（history 傳遞、長度控制、指代指示、Query Rewriting）、**主 RAG 雙 Prompt**（調查員打包 + 判官判定）、**合約專家**（contract_risk_agent + 15 年法務 prompt）、**多查詢檢索**（輔助問句合併）與**相關文件**均已實作並可運作，完整度高。  
  剩餘為可選的說明補強（文件建議、README 對齊競賽主題），不影響現有邏輯與展示。
